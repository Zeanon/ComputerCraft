local reactors = 2
local monitor = monitor = periphSearch("monitor")

os.loadAPI("lib/gui")
os.loadAPI("lib/surface")

function getOutputs()
  
end  

while true do
  parallel.waitForAny(getOutputs)
end
